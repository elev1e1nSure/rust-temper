<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="RustPatch.Views.CommandPicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Button x:Name="ToggleButton"
            HorizontalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            Background="{ThemeResource ControlFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}">
        <Button.Flyout>
            <!--
                Flyout handles correct below-the-control placement (with edge flipping) and
                mutual-exclusion with other open flyouts natively — unlike a retemplated
                ComboBox popup, which fights back against any positioning override.
            -->
            <Flyout x:Name="ItemsFlyout" Placement="Bottom" ShouldConstrainToRootBounds="False">
                <Flyout.FlyoutPresenterStyle>
                    <Style TargetType="FlyoutPresenter">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="MaxHeight" Value="504" />
                    </Style>
                </Flyout.FlyoutPresenterStyle>
                <ListView x:Name="ItemsList"
                          ItemsSource="{x:Bind ItemsSource, Mode=OneWay}"
                          DisplayMemberPath="Name"
                          IsItemClickEnabled="True"
                          ItemClick="OnItemClick"
                          Width="{x:Bind ToggleButton.ActualWidth, Mode=OneWay}" />
            </Flyout>
        </Button.Flyout>

        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="SelectedText" Grid.Column="0" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
            <FontIcon Grid.Column="1" Glyph="&#xE70D;" FontSize="12" VerticalAlignment="Center" />
        </Grid>
    </Button>
</UserControl>
